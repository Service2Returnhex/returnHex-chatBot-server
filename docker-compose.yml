version: "3.9"

services:
  chatbot-backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: chatbot-backend:v1
    container_name: chatbot-backend-v1
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: unless-stopped
