services:
  chatbot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chatbot-server
    ports:
      - "5030:5030"  
    env_file: ./.env
    volumes:
      - ./:/app
      - /app/node_modules
    networks:
      - bot-server


networks:
  bot-server:
    driver: bridge