import { RequestHandler } from "express";
import { catchAsync } from "../../utility/cathcAsync";

const leadFormCreate:RequestHandler=catchAsync(
    async (req:Request, res:Response)=>{
        
    }
)